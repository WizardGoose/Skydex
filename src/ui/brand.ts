/** Display identity. Storage uses explicit keys and a tested migration. */
export const SITE_NAME = "Skydex";

/** Accent starts after "Sky", independently of capitalization. */
export const SITE_NAME_ACCENT_AT = 3;
